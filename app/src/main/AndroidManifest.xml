<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dezhen.aesop">
    <!--permission-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required only if your app needs to access images or photos
     that other apps created. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Required only if your app needs to access videos
         that other apps created. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Required only if your app needs to access audio files
         that other apps created. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- If your app doesn't need to access media files that other apps created,
         set the "maxSdkVersion" attribute to "28" instead. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>



    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/reading_book"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/reading_book"
        android:supportsRtl="true"
        android:theme="@style/Theme.myTheme">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3967893153379621~5975145824"/>
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:exported="true" android:name=".bbc.BBC_Content_0" />
        <activity android:name=".bbc.BBC_Content_1" android:exported="true" />
        <activity android:name=".bbc.BBC_Content_2" android:exported="true" />
        <activity android:name=".bbc.BBC_Content_3" android:exported="true" />
        <activity android:name=".bbc.BBC_Content_4" android:exported="true" />

        <activity android:name=".collection.Co_Content_0" android:exported="true"/>
        <activity android:name=".collection.Co_Content_1" android:exported="true"/>
        <activity android:name=".collection.Co_Content_2" android:exported="true"/>
        <activity android:name=".collection.Co_Content_3" android:exported="true"/>
        <activity android:name=".collection.Co_Content_4" android:exported="true"/>

    </application>

</manifest>